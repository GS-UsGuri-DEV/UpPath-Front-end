// ChangePassword removed: password management now handled by backend flows.
// Keeping a small stub file so imports (if any) don't break the build.
export default function ChangePassword(): null {
  return null
}
